Хакатон — план по пунктам (под ваш текущий проект “Desktop Pet” на Python + Tkinter)

1) Формулировка идеи (1–2 предложения): “Десктоп‑питомец, который живёт поверх рабочего стола, требует ухода, реагирует на действия и меняется со временем”.  
2) MVP‑функции (обязательное): имя питомца; 3 характеристики (голод/энергия/настроение); 3 действия (кормить/играть/спать); авто‑изменение со временем; отображение состояния (HUD).  
3) Интерфейс: окно питомца (гифка + перетаскивание), HUD с 3 компактными полосками (перетаскивается отдельно), меню действий (только кнопки).  
4) Логика состояния: единые правила изменения статов по таймеру, пределы 0–100, последствия “если долго не ухаживать” (грустит/болеет/засыпает).  
5) Хранение данных: сохранение имени, статов, режима “откусывания” и позиций окон (питомец/HUD) в json; восстановление при старте.  : ХРАНЕНИЕ ДАННЫХ: Сохранение имени, статов, режимов, позиций окон --- PASHA:
   - PySql
6) “Изюминка” для эмоций: уникальные фразы/реакции питомца (короткие реплики при действиях и плохом самочувствии), эмоция/анимация зависит от статов.  
7) Доп.баллы:  
   - AI: генерация реплик питомца (можно офлайн‑шаблоны + опционально AI).  
   - Внешний API: погода влияет на настроение/энергию (например, пасмурно — настроение падает быстрее).  
8) Надёжность: проверка что всё запускается одной командой; обработка ошибок (нет интернета — API отключается без краша); никаких бесконечных `mainloop()`; стабильное обновление через таймер.  
9) Репозиторий: публичный GitHub; README (идея, команда, запуск, демо); .gitignore; requirements.txt; лицензия (по желанию).  
10) Доступность для теста: если это десктоп‑приложение — приложить релиз (exe) или чёткую инструкцию “python main.py”; добавить короткое видео/гиф демонстрации в README.  
11) Презентация 5 минут: 20–30 сек идея; 3 минуты демо (создание/действия/как меняется со временем); 1 мин “как сделали” (HUD, сохранение, таймер, API/AI); 30 сек планы/итог.

Вопросы (чтобы подогнать под требования и баллы)
1) Где вы будете “онлайн доступность” обеспечивать: бот/веб/андроид, или оставляем десктоп и делаем “доступно для теста” через релиз + видео?  
2) Нужно ли добавлять создание питомца с именем при первом запуске (окно ввода), или имя уже хранится/захардкожено?  
3) Хотите ли вы добирать доп.баллы: AI‑реплики, внешний API (погода), или достаточно просто хорошо отполировать MVP?  
4) Команда сколько человек и кто что делает (код/дизайн/README/презентация), чтобы я разложил задачи по ролям?
